import java.rmi.server.Operation;
import java.util.*;
import java.util.stream.Collectors;

public class StreamMethod {
    public static void main(String[] args) {
        
        //filter(Predicate)
        //boolean value return function

        //map(Function)//return value
    //     //each element Operation
    //     List<String> names=List.of("Aman","Ankit","Abhinav","Durgesh");
    //    List<String> newNames= names.stream().filter(e->e.startsWith("A")).collect(Collectors.toList());
    //    System.out.println(newNames);
    // List<Integer> numbers=List.of(23,4,5,2,7,2);
    // List<Integer> new1=numbers.stream().map(i->i*i).collect(Collectors.toList());
    // System.out.println(new1);
    // new1.stream().forEach(System.out::println);
    // }
}
